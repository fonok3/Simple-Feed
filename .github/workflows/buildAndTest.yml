name: Build and Test
branding:
  icon: 'check-circle'
  color: 'green'
  
on: pull_request

jobs:
  BuildAndTest:
    runs-on: macOS-latest
    steps:
      - name: Build
        run: xcodebuild -scheme "Simple Feed" build
      - name: Test
        run: xcodebuild test -workspace "Simple Feed"


